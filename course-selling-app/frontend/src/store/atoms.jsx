import {atom} from "recoil"

export const Admin = atom({
    key:'Admin',
    default:{}
});

export const User = atom({
    key:'User',
    default:{}
});

export const Course = atom({
    key:'Course',
    default:{}
});